{
  "name": "frontendmaxxing-tools",
  "private": true,
  "description": "Dev tooling for the vault (render-smoke harness). Snippets stay zero-dep; this never ships to users. render-smoke.mjs is ESM via its .mjs extension; audit.js stays CommonJS, so this package intentionally omits \"type\":\"module\".",
  "devDependencies": {
    "playwright-core": "1.60.0",
    "pixelmatch": "7.2.0",
    "pngjs": "7.0.0"
  }
}
